services:
  universal-flex-agent:
    build: .
    container_name: universal-flex-agent
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
      - ./workspace:/workspace
      - ${HOST_PROJECTS_DIR:-/Users}:/projects:ro
    restart: unless-stopped
