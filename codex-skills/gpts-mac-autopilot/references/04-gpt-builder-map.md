# GPT Builder Map

Что куда вставлять:

- `gpts/system_instructions.txt` -> поле `Instructions`
- файлы из `gpts/knowledge/` -> `Knowledge`
- `openapi.gpts.yaml` -> `Actions`
- `AGENT_TOKEN` из `.env` -> auth token для action

Первый тестовый вызов: `getCapabilities`.
