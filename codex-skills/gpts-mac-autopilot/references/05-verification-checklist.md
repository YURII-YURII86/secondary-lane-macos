# Verification Checklist

Минимум перед финальным успехом:

- `python3.13 --version` работает
- `ngrok version` работает
- `.env` существует и заполнен
- панель запущена
- tunnel живой
- `openapi.gpts.yaml` содержит реальный URL
- GPT создан
- auth настроен
- `getCapabilities` успешно вызывается
