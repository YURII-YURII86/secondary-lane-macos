# Runbook: GPT Exists But Broken

Используй, если GPT уже был создан, но не работает.

Сначала проверь:

- жив ли panel
- жив ли tunnel
- актуален ли URL в `openapi.gpts.yaml`
- правильный ли token в `Actions`
- проходит ли простой action-вызов
