# Browser Protocol

Если можно открыть нужную страницу автоматически, делай это.

Для macOS предпочитай:

```bash
open "<url>"
```

После открытия страницы всегда скажи пользователю только одно ближайшее действие, а не список из десяти пунктов.
